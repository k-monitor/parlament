fastapi>=0.115
uvicorn[standard]>=0.30
# Per-speech readability (LIX/RIX) + lexical diversity (TTR/MATTR) — READ-1..7,
# app/readability.py. Pure stdlib, no model: the readability half works on a bare
# install. Its Hungarian long-word calibration (threshold 8, not Björnsson's 6) is
# read from the package rather than hard-coded, so a recalibration ships with an
# upgrade — pin-check `saphes.recommended_threshold("hu")` when bumping it.
saphes>=0.1
# Word-cloud lemmatization + entity extraction (WCLOUD-2). HuSpaCy pulls in spaCy;
# the model itself is a separate wheel. The project default is the transformer
# (hu_core_news_trf, best accuracy) which in practice runs ONLY on Modal
# (PARLAMONITOR_WORDCLOUD_BACKEND=modal, see modal_app.py): its latest build
# targets spaCy 3.7 + 2023-era spacy-transformers/tokenizers whose wheels stop at
# Python 3.11, so it won't install into a modern local venv. For local NLP use
# the ~127 MB CPU model and point the app at it with
# PARLAMONITOR_HUSPACY_MODEL=hu_core_news_md:
#   python -c "import huspacy; huspacy.download('hu_core_news_md')"
# The word cloud degrades to a regex tokenizer when the model is absent, so this
# is optional for a minimal build (PARLAMONITOR_WORDCLOUD_BACKEND=regex forces it off).
huspacy>=0.5
# Offload the word-cloud NER/lemmatization to Modal GPU/CPU workers when
# PARLAMONITOR_WORDCLOUD_BACKEND=modal (WCLOUD-6). Only the lightweight client is
# needed on the host; the heavy spaCy/model live in the Modal image (modal_app.py).
# Optional — the build falls back to local huspacy/regex when it is absent.
modal>=0.64
# stdlib sqlite3 (FTS5) is used directly — no ORM. Tests:
pytest>=8.0
httpx>=0.27
